{
  "name": "wavez-discord-presence",
  "version": "1.6.0",
  "description": "Local bridge that shows your currently playing wavez.fm tracks/status as Discord Rich Presence.",
  "license": "MIT",
  "main": "src/presence.js",
  "bin": {
    "wavez-discord-presence": "src/presence.js"
  },
  "files": [
    "src",
    "config.example.json"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/presence.js",
    "test": "node tests/metadata.test.js && node tests/artwork.test.js && node tests/arbiter.test.js && node tests/port.test.js",
    "version": "node -e \"const f='wavez-discord-presence.user.js',fs=require('fs'),v=require('./package.json').version;fs.writeFileSync(f,fs.readFileSync(f,'utf8').replace(/(@version\\s+)[\\d.]+/,(m,p)=>p+v));console.log('userscript @version -> '+v)\" && git add wavez-discord-presence.user.js"
  },
  "keywords": [
    "wavez",
    "discord",
    "discord-rpc",
    "rich-presence",
    "userscript"
  ],
  "homepage": "https://github.com/fluteds/wavez-discord-presence",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fluteds/wavez-discord-presence.git"
  },
  "bugs": "https://github.com/fluteds/wavez-discord-presence/issues",
  "dependencies": {
    "@xhayper/discord-rpc": "^1.2.0"
  },
  "//overrides": "The pinned @discordjs/rest@2.6.1 (via @xhayper/discord-rpc) hard-requires undici 6.24.1, which is under the <=6.26.0 advisories; this lifts it to a patched 6.27.x. @discordjs/rest@2.6.3 already asks for ^6.27.0 itself, so drop this override once the lockfile resolves @discordjs/rest >=2.6.3 (bump @xhayper/discord-rpc). npm audit is clean with the override in place.",
  "overrides": {
    "undici": "^6.28.0"
  }
}
